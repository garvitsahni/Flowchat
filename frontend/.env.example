# Frontend env template — copy to frontend/.env for local dev, or set in
# Vercel/Netlify dashboard for hosted builds.
#
# Set to the deployed backend URL (no trailing slash) when NOT using the
# local Vite dev proxy:
VITE_API_BASE=
# true = serve mock responses (no backend needed); false/default = call /query
VITE_USE_MOCK=false